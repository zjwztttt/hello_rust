[package]
name = "Mosquito"
version = "0.1.0"
edition = "2024"

[dependencies]
clipboard = "0.5.0"  # Windows 剪贴板库
url = "2.5.8" # 校验URL合法性
toml = "0.8" # TOML 解析
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"